@model IEnumerable<Rocky_1.Models.Category_1>

<style>

    div button{
        margin-left: 3px;
        margin-right: 3px;
       
    }
</style>

<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary">Category List</h2>
        </div>
        <div class="col-6 text-end">
            <a asp-controller="Category_1" asp-action="Create" class="btn btn-primary">
                <i class="fa-solid fa-plus"></i> &nbsp; Create New Category
            </a>
        </div>
    </div>

    <br /><br />


    @if (Model.Count() > 0)
    {
        <table class="table table-bordered table-striped" style="width:100%">
            <thead>
                <tr>
                    <th>
                        Category Name
                    </th>
                    <th>
                        Order Name
                    </th>
                    <th class="text-center">Edit / Delete</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var obj in Model)
                {
                    <tr>
                        <td width="45%">@obj.Name</td>
                        <td width="30%">@obj.DisplayOrder</td>
                        <td class="text-center">
                            <div class="w-85 btn-group" role="group">

                                <a asp-controller="Category_1" asp-route-id="@obj.Id" asp-action="Edit" type="submit" class="btn btn-info mx-1"> <i class="fa-regular fa-pen-to-square"></i>&nbsp; Edit</a>

                                <a asp-controller="Category_1" asp-route-id="@obj.Id" asp-action="Delete" type="submit" class="btn btn-danger mx-1 ">Delete &nbsp;<i class="fas fa-trash-alt"></i></a>

                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    }

    else
    {
        <p> No Category Exists.</p>
    }

</div>


